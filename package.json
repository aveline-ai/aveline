{
  "scripts": {
    "format": "npm run format:elm && npm run format:gleam && npm run format:elixir",
    "format:elm": "elm-format assets/src --yes",
    "format:gleam": "gleam format",
    "format:elixir": "mix format"
  },
  "devDependencies": {
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7",
    "elm-test": "^0.19.1-revision16",
    "esbuild": "^0.25.0",
    "esbuild-plugin-elm": "^0.0.12",
    "playwright": "^1.63.0"
  }
}
