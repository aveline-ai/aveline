name = "aveline"
version = "1.0.0"
target = "erlang"

# Note: Dependencies are managed in mix.exs
# These are duplicated here for gleam CLI tools (check, format, test)
[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
gleam_json = ">= 3.0.0 and < 4.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
